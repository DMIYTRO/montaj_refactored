# Автозапуск скрипта в InDesign

Инструкция по настройке автоматического мониторинга папки и обработки файлов прямо в InDesign.

---

## Как это работает

**AutoMontaj.jsx** - это фоновый скрипт, который:
- ✅ Запускается автоматически при старте InDesign
- ✅ Каждые 5 минут проверяет папку на новые файлы
- ✅ Автоматически обрабатывает найденные PDF
- ✅ Работает в фоне, не мешая другой работе
- ✅ Имеет панель управления для контроля

---

## Установка

### Вариант 1: Startup Scripts (Автозапуск)

**Windows:**
```
1. Найти папку Startup Scripts InDesign:
   C:\Users\<username>\AppData\Roaming\Adobe\InDesign\Version XX.0\ru_RU\Scripts\Startup Scripts\

   Быстрый путь:
   - Открыть InDesign
   - Окно → Утилиты → Сценарии (Window → Utilities → Scripts)
   - Правой кнопкой на "Startup Scripts" → Показать в проводнике

2. Скопировать в эту папку:
   - AutoMontaj.jsx
   - Config.jsx
   - Utils.jsx
   - Imposition.jsx
   - JDF.jsx
   - Logger.jsx

3. Перезапустить InDesign
```

**macOS:**
```
1. Найти папку Startup Scripts:
   ~/Library/Preferences/Adobe InDesign/Version XX.0/ru_RU/Scripts/Startup Scripts/

   Быстрый путь:
   - InDesign → Scripts → Reveal in Finder → Startup Scripts

2. Скопировать все файлы (см. выше)

3. Перезапустить InDesign
```

### Вариант 2: Scripts Panel (Ручной запуск)

**Windows:**
```
C:\Users\<username>\AppData\Roaming\Adobe\InDesign\Version XX.0\ru_RU\Scripts\Scripts Panel\
```

**macOS:**
```
~/Library/Preferences/Adobe InDesign/Version XX.0/ru_RU/Scripts/Scripts Panel/
```

Скопировать файлы и запускать вручную из панели Scripts.

---

## Настройка

Откройте **AutoMontaj.jsx** и измените настройки в начале файла:

```javascript
var AUTO_CONFIG = {
    enabled: true,                    // Включить автообработку
    checkInterval: 300000,            // Проверка каждые 5 минут (в миллисекундах)
    sourceFolder: "//10.20.2.60/work/input/",  // Папка для мониторинга
    runOnStartup: true,               // Запустить сразу при старте InDesign
    showNotifications: true,          // Показывать уведомления
    autoCloseDocuments: true,         // Закрывать документы после обработки
    maxFilesPerRun: 50,              // Максимум файлов за один раз

    // Рабочие часы (null = 24/7)
    workingHours: {
        enabled: true,
        start: 8,                     // Начало: 8:00
        end: 20,                      // Конец: 20:00
        daysOfWeek: [1, 2, 3, 4, 5]  // Пн-Пт (0=Вс, 6=Сб)
    }
};
```

### Примеры настройки интервала:

```javascript
checkInterval: 60000      // 1 минута
checkInterval: 180000     // 3 минуты
checkInterval: 300000     // 5 минут (по умолчанию)
checkInterval: 600000     // 10 минут
checkInterval: 1800000    // 30 минут
checkInterval: 3600000    // 1 час
```

### Работа 24/7:

```javascript
workingHours: {
    enabled: false  // Отключить ограничения по времени
}
```

### Только выходные:

```javascript
workingHours: {
    enabled: true,
    start: 0,
    end: 24,
    daysOfWeek: [0, 6]  // Суббота и Воскресенье
}
```

---

## Управление

### Панель управления

После установки в InDesign появится меню:
```
Automation → AutoMontaj Control Panel
```

**Панель позволяет:**
- ▶ **Start** - запустить автоматическую обработку
- ⏸ **Stop** - остановить
- ▶▶ **Run Now** - запустить проверку немедленно
- 📊 Видеть статистику (обработано, ошибки)
- ⚙ Изменять интервал и папку

### Консоль ExtendScript

Открыть: `Window → Utilities → ExtendScript Toolkit`

Лог будет показывать:
```
=================================================
AutoMontaj - Automatic Folder Monitor
=================================================
Source: //10.20.2.60/work/input/
Interval: 300 seconds
Enabled: true
[AutoMontaj] Idle task created. Checking every 300 seconds
=================================================
[AutoMontaj] Checking folder: //10.20.2.60/work/input/
[AutoMontaj] Found 5 file(s)
[AutoMontaj] Processing: file1.pdf
[AutoMontaj] Processing: file2.pdf
[AutoMontaj] Finished: Success=5, Errors=0
```

---

## Интеграция с Main.jsx

Чтобы AutoMontaj использовал полную логику обработки из Main.jsx:

### Способ 1: Вызов Main.jsx

```javascript
// В AutoMontaj.jsx, в функции processFolder():

// Вместо простого перемещения файлов, запустить Main.jsx
app.doScript(new File(AUTO_CONFIG.sourceFolder + "../Main.jsx"), ScriptLanguage.JAVASCRIPT);
```

### Способ 2: Встроить логику

Скопировать код из Main.jsx прямо в функцию `processFolder()` файла AutoMontaj.jsx.

---

## Проверка работы

### 1. Тестовый запуск

1. Открыть InDesign
2. Окно → Утилиты → Сценарии
3. Найти AutoMontaj.jsx
4. Двойной клик для запуска

Должна появиться панель управления (если не закомментирована строка `createControlPanel()`).

### 2. Проверка логов

**ExtendScript Toolkit Console:**
```
Инструменты → Adobe InDesign → ExtendScript Toolkit
```

**processing_log.txt:**
```
C:\path\to\sourceFolder\processing_log.txt
```

### 3. Проверка Idle Task

```javascript
// Запустить в ExtendScript Toolkit:
app.idleTasks.everyItem().name
// Должен показать: ["AutoMontaj_Monitor"]
```

---

## Отключение

### Временное отключение

1. Открыть панель: `Automation → AutoMontaj Control Panel`
2. Нажать **Stop**

Или изменить в AutoMontaj.jsx:
```javascript
enabled: false
```

### Полное удаление

1. Удалить файлы из папки Startup Scripts
2. Перезапустить InDesign

Или в ExtendScript Toolkit:
```javascript
// Удалить idle task
app.idleTasks.itemByName("AutoMontaj_Monitor").remove();

// Удалить меню
app.scriptMenuActions.itemByName("AutoMontaj Control Panel").remove();
```

---

## Безопасность

### Защита от параллельных запусков

Скрипт проверяет флаг `autoMontajState.isRunning` перед началом обработки.

### Ограничение файлов

```javascript
maxFilesPerRun: 50  // Не обрабатывать больше 50 файлов за раз
```

Это предотвращает зависание InDesign при большом количестве файлов.

### Автозакрытие документов

```javascript
autoCloseDocuments: true  // Закрывать документы после обработки
```

Освобождает память и предотвращает накопление открытых документов.

---

## Отладка проблем

### Скрипт не запускается при старте

1. Проверить путь к Startup Scripts:
```
InDesign → Scripts → Reveal in Explorer/Finder → Startup Scripts
```

2. Проверить наличие всех файлов:
- AutoMontaj.jsx ✓
- Config.jsx ✓
- Utils.jsx ✓
- Imposition.jsx ✓
- JDF.jsx ✓
- Logger.jsx ✓

3. Проверить консоль на ошибки:
```
ExtendScript Toolkit → Adobe InDesign
```

### Папка не мониторится

1. Проверить путь в AUTO_CONFIG:
```javascript
sourceFolder: "//10.20.2.60/work/input/"  // Правильный путь?
```

2. Проверить доступ к сетевой папке:
```javascript
// Запустить в ExtendScript Toolkit:
var folder = new Folder("//10.20.2.60/work/input/");
alert(folder.exists);  // Должно быть true
```

### Idle Task не работает

1. Проверить создание:
```javascript
app.idleTasks.everyItem().name
```

2. Проверить интервал:
```javascript
checkInterval: 300000  // Не слишком большой?
```

3. Перезапустить InDesign

---

## Альтернативные варианты

### Вариант 1: Folder Action (macOS)

Автоматически запускать скрипт при появлении файлов в папке.

### Вариант 2: File Watcher Service

Создать отдельный сервис для мониторинга папки.

### Вариант 3: InDesign Server

Для промышленного масштаба использовать InDesign Server с SOAP/REST API.

---

## Производительность

### Рекомендации:

- ✅ Интервал не менее 1 минуты (60000 мс)
- ✅ Максимум 50 файлов за раз
- ✅ Автозакрытие документов включено
- ✅ Работать только в рабочие часы
- ✅ Мониторить логи первую неделю

### Оптимизация:

```javascript
// Быстрая проверка (только в рабочее время)
checkInterval: 60000,              // 1 минута
maxFilesPerRun: 10,               // 10 файлов
autoCloseDocuments: true,
workingHours: { enabled: true }

// Редкая проверка (круглосуточно)
checkInterval: 600000,            // 10 минут
maxFilesPerRun: 100,
autoCloseDocuments: true,
workingHours: { enabled: false }
```

---

## Контакты

Для поддержки и вопросов:
- GitHub Issues: https://github.com/DMIYTRO/montaj_refactored/issues